# Anthropic (Claude API) — for pre-call briefs and 90-Day Value Reports
ANTHROPIC_API_KEY=

# No database setup needed — SQLite file is created automatically at /data/vai-crm.db
# No Dynamics API credentials needed — use the /import page to upload CSV exports
